version: "3.9"

services:
  kniffel:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kniffel-multiplayer
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
